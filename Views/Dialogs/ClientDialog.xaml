<Window x:Class="wpfhikip.Views.Dialogs.ClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        Height="600"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Background="#f8fafc">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,0,0,1">
            <Grid Margin="24,16">
                <StackPanel>
                    <TextBlock Text="{Binding DialogTitle}"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource SEC_DarkGrey}" />
                    <TextBlock Text="{Binding DialogSubtitle}"
                               FontSize="14"
                               Foreground="{StaticResource SEC_MidGrey}"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Margin="24,16">
            <StackPanel>
                <!-- Basic Information Section -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Margin="0,0,0,16">
                    <StackPanel Margin="20">
                        <TextBlock Text="Basic Information"
                                   Style="{StaticResource SectionTitleStyle}"
                                   Margin="0,0,0,16" />

                        <!-- Client Name -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Name *"
                                       VerticalAlignment="Center"
                                       FontWeight="Medium" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.Name, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}" />
                        </Grid>

                        <!-- Contact Person -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Contact Person"
                                       VerticalAlignment="Center"
                                       FontWeight="Medium" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.ContactPerson, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}" />
                        </Grid>

                        <!-- Email -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Email"
                                       VerticalAlignment="Center"
                                       FontWeight="Medium" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.Email, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}" />
                        </Grid>

                        <!-- Phone -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Phone"
                                       VerticalAlignment="Center"
                                       FontWeight="Medium" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.Phone, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}" />
                        </Grid>

                        <!-- Address -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Address"
                                       VerticalAlignment="Top"
                                       FontWeight="Medium"
                                       Margin="0,8,0,0" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.Address, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Height="80"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" />
                        </Grid>

                        <!-- Notes -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Notes"
                                       VerticalAlignment="Top"
                                       FontWeight="Medium"
                                       Margin="0,8,0,0" />
                            <TextBox Grid.Column="1"
                                     Text="{Binding Client.Notes, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Height="100"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Validation Errors -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Background="#fef2f2"
                        BorderBrush="#fecaca">
                    <StackPanel Margin="16">
                        <TextBlock Text="Please correct the following errors:"
                                   FontWeight="Medium"
                                   Foreground="#dc2626"
                                   Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding ValidationErrors}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               Foreground="#dc2626"
                                               Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="2"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,1,0,0">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Info Text -->
                <TextBlock Grid.Column="0"
                           Text="* Required fields"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource SEC_MidGrey}"
                           FontStyle="Italic" />

                <!-- Buttons -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Width="80"
                            Margin="0,0,8,0" />
                    <Button Content="{Binding SaveButtonText}"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Width="80" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>