<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Compact Dropdown Menu Button Style for SiteManager -->
    <Style x:Key="DropdownMenuButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Content"
                Value="⚙️ Actions ▼" />
        <Setter Property="Width"
                Value="90" />
        <Setter Property="Margin"
                Value="4,0,0,0" />
        <Setter Property="Effect"
                Value="{x:Null}" />
    </Style>

    <!-- Compact Primary Button Style for SiteManager -->
    <Style x:Key="CompactPrimaryButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource PrimaryButtonStyle}">
        <Setter Property="Margin"
                Value="0,0,4,0" />
        <Setter Property="Effect"
                Value="{x:Null}" />
        <Setter Property="Padding"
                Value="8,6" />
        <Setter Property="FontSize"
                Value="12" />
    </Style>

    <!-- Client List Item Style -->
    <Style x:Key="ClientListItemStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="White" />
        <Setter Property="BorderBrush"
                Value="{StaticResource SEC_LightGrey}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="CornerRadius"
                Value="4" />
        <Setter Property="Margin"
                Value="0,2" />
        <Setter Property="Padding"
                Value="12,8" />
    </Style>

    <!-- Client Name Text Style -->
    <Style x:Key="ClientNameTextStyle"
           TargetType="TextBlock">
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="FontSize"
                Value="14" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_DarkGrey}" />
    </Style>

    <!-- Client Info Text Style -->
    <Style x:Key="ClientInfoTextStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="11" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_MidGrey}" />
        <Setter Property="Margin"
                Value="0,4,0,0" />
    </Style>

    <!-- Site List Item Style -->
    <Style x:Key="SiteListItemStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="White" />
        <Setter Property="BorderBrush"
                Value="{StaticResource SEC_LightGrey}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="CornerRadius"
                Value="4" />
        <Setter Property="Margin"
                Value="0,2" />
        <Setter Property="Padding"
                Value="12,8" />
    </Style>

    <!-- Site Name Text Style -->
    <Style x:Key="SiteNameTextStyle"
           TargetType="TextBlock">
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="FontSize"
                Value="14" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_DarkGrey}" />
    </Style>

    <!-- Site Info Text Style -->
    <Style x:Key="SiteInfoTextStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="11" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_MidGrey}" />
        <Setter Property="Margin"
                Value="0,4,0,0" />
    </Style>

    <!-- Transparent ListBox Style -->
    <Style x:Key="TransparentListBoxStyle"
           TargetType="ListBox">
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="BorderThickness"
                Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
                Value="Disabled" />
    </Style>

    <!-- Header Title Style -->
    <Style x:Key="HeaderTitleStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="28" />
        <Setter Property="FontWeight"
                Value="Bold" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_DarkGrey}" />
    </Style>

    <!-- Header Subtitle Style -->
    <Style x:Key="HeaderSubtitleStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="14" />
        <Setter Property="Foreground"
                Value="{StaticResource SEC_MidGrey}" />
        <Setter Property="Margin"
                Value="0,4,0,0" />
    </Style>

    <!-- DataGrid Cell Text Style -->
    <Style x:Key="DataGridCellTextStyle"
           TargetType="TextBlock">
        <Setter Property="HorizontalAlignment"
                Value="Center" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="FontSize"
                Value="12" />
    </Style>

    <!-- DataGrid Cell TextBox Style -->
    <Style x:Key="DataGridCellTextBoxStyle"
           TargetType="TextBox">
        <Setter Property="HorizontalContentAlignment"
                Value="Center" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="FontSize"
                Value="12" />
    </Style>

    <!-- Action Button Panel Style -->
    <Style x:Key="ActionButtonPanelStyle"
           TargetType="StackPanel">
        <Setter Property="Orientation"
                Value="Horizontal" />
        <Setter Property="Margin"
                Value="0,8,0,0" />
        <Setter Property="VerticalAlignment"
                Value="Bottom" />
    </Style>

    <!-- Main Background Style -->
    <Style x:Key="MainBackgroundStyle"
           TargetType="Grid">
        <Setter Property="Background"
                Value="#f8fafc" />
    </Style>

    <!-- Header Border Style -->
    <Style x:Key="HeaderBorderStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource SEC_White}" />
        <Setter Property="BorderBrush"
                Value="{StaticResource SEC_LightGrey}" />
        <Setter Property="BorderThickness"
                Value="0,0,0,1" />
    </Style>

    <!-- Action Bar Border Style -->
    <Style x:Key="ActionBarBorderStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource SEC_White}" />
        <Setter Property="BorderBrush"
                Value="{StaticResource SEC_LightGrey}" />
        <Setter Property="BorderThickness"
                Value="0,1,0,0" />
    </Style>

    <!-- Custom DataGrid Row Style for SiteManager -->
    <Style x:Key="SiteManagerDataGridRowStyle"
           TargetType="DataGridRow">
        <Setter Property="Background"
                Value="{Binding CellColor}" />
    </Style>

</ResourceDictionary>