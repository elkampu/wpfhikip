<Window x:Class="wpfhikip.Views.Dialogs.StatusDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:wpfhikip.Converters"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="900"
        Width="1200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600"
        MinWidth="700">
    <Window.Resources>
        <!-- Converters -->
        <converters:EnumToStringConverter x:Key="EnumToStringConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

        <!-- Modern Card Style -->
        <Style x:Key="ModernCardStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="{StaticResource SEC_White}" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource SEC_LightGrey}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="CornerRadius"
                    Value="8" />
            <Setter Property="Padding"
                    Value="16" />
            <Setter Property="Margin"
                    Value="8" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8"
                                      ShadowDepth="2"
                                      Opacity="0.1" />
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Styles -->
        <Style x:Key="ModernButtonStyle"
               TargetType="Button">
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Padding"
                    Value="12,8" />
            <Setter Property="Margin"
                    Value="4" />
            <Setter Property="FontWeight"
                    Value="Medium" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4"
                                      ShadowDepth="1"
                                      Opacity="0.2" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Opacity"
                            Value="0.9" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="0.98"
                                            ScaleY="0.98" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background"
                    Value="{StaticResource SEC_MidBlue}" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_White}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="{StaticResource SEC_LightBlue}" />
                </Trigger>
                <Trigger Property="IsPressed"
                         Value="True">
                    <Setter Property="Background"
                            Value="{StaticResource SEC_NavyBlue}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background"
                    Value="#16a34a" />
            <Setter Property="Foreground"
                    Value="White" />
        </Style>

        <Style x:Key="SecondaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background"
                    Value="{StaticResource SEC_MidGrey}" />
            <Setter Property="Foreground"
                    Value="White" />
        </Style>

        <!-- Section Title Style -->
        <Style x:Key="SectionTitleStyle"
               TargetType="TextBlock">
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontSize"
                    Value="16" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_DarkGrey}" />
            <Setter Property="Margin"
                    Value="0,0,0,12" />
        </Style>

        <!-- Stats Card Style -->
        <Style x:Key="StatsCardStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="{StaticResource SEC_White}" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource SEC_LightGrey}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="CornerRadius"
                    Value="6" />
            <Setter Property="Padding"
                    Value="16" />
            <Setter Property="Margin"
                    Value="4" />
        </Style>

        <!-- Modern Text Styles -->
        <Style x:Key="ModernTextBlockStyle"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_DarkGrey}" />
            <Setter Property="FontFamily"
                    Value="Segoe UI" />
            <Setter Property="FontSize"
                    Value="12" />
        </Style>

        <Style x:Key="HeaderTextBlockStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize"
                    Value="24" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Margin"
                    Value="0,0,0,8" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_DarkGrey}" />
        </Style>

        <Style x:Key="SubHeaderTextBlockStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_MidGrey}" />
            <Setter Property="Margin"
                    Value="0,0,0,0" />
        </Style>

        <Style x:Key="SectionHeaderStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Margin"
                    Value="0,0,0,10" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_DarkGrey}" />
        </Style>

        <Style x:Key="LabelTextBlockStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontSize"
                    Value="11" />
            <Setter Property="Margin"
                    Value="0,0,10,5" />
            <Setter Property="VerticalAlignment"
                    Value="Top" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_MidGrey}" />
        </Style>

        <Style x:Key="ValueTextBlockStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize"
                    Value="11" />
            <Setter Property="FontFamily"
                    Value="Consolas" />
            <Setter Property="Margin"
                    Value="0,0,0,5" />
            <Setter Property="TextWrapping"
                    Value="Wrap" />
        </Style>

        <!-- Log Level to Color and Icon Converter -->
        <Style x:Key="LogIconStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontFamily"
                    Value="Segoe UI Symbol" />
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="Margin"
                    Value="0,0,8,0" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}"
                             Value="Success">
                    <Setter Property="Foreground"
                            Value="#FF28A745" />
                    <Setter Property="Text"
                            Value="✓" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Warning">
                    <Setter Property="Foreground"
                            Value="#FFFFC107" />
                    <Setter Property="Text"
                            Value="⚠" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Error">
                    <Setter Property="Foreground"
                            Value="#FFDC3545" />
                    <Setter Property="Text"
                            Value="✗" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Info">
                    <Setter Property="Foreground"
                            Value="#FF17A2B8" />
                    <Setter Property="Text"
                            Value="→" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Protocol Group Header Style -->
        <Style x:Key="ProtocolHeaderStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource ModernTextBlockStyle}">
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Foreground"
                    Value="{StaticResource SEC_MidBlue}" />
            <Setter Property="Margin"
                    Value="0,10,0,5" />
        </Style>

        <!-- Log Entry Style -->
        <Style x:Key="LogEntryStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="#f9fafb" />
            <Setter Property="CornerRadius"
                    Value="4" />
            <Setter Property="Padding"
                    Value="12,8" />
            <Setter Property="Margin"
                    Value="0,2" />
            <Setter Property="BorderThickness"
                    Value="2,0,0,0" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}"
                             Value="Success">
                    <Setter Property="BorderBrush"
                            Value="#FF28A745" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Warning">
                    <Setter Property="BorderBrush"
                            Value="#FFFFC107" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Error">
                    <Setter Property="BorderBrush"
                            Value="#FFDC3545" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}"
                             Value="Info">
                    <Setter Property="BorderBrush"
                            Value="#FF17A2B8" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Protocol Status Card Style -->
        <Style x:Key="ProtocolStatusCardStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="{StaticResource SEC_White}" />
            <Setter Property="CornerRadius"
                    Value="6" />
            <Setter Property="Padding"
                    Value="12" />
            <Setter Property="Margin"
                    Value="4" />
            <Setter Property="BorderThickness"
                    Value="2" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4"
                                      ShadowDepth="1"
                                      Opacity="0.1" />
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Connection Info Style -->
        <Style x:Key="ConnectionInfoStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="#f3f4f6" />
            <Setter Property="CornerRadius"
                    Value="6" />
            <Setter Property="Padding"
                    Value="16" />
            <Setter Property="Margin"
                    Value="0,8,0,0" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="BorderBrush"
                    Value="{StaticResource SEC_LightGrey}" />
        </Style>
    </Window.Resources>

    <Grid Background="#f8fafc">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,0,0,1">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Protocol Status &amp; Activity Monitor"
                               Style="{StaticResource HeaderTextBlockStyle}" />
                    <TextBlock Text="Real-time monitoring and diagnostic information"
                               Style="{StaticResource SubHeaderTextBlockStyle}" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="📋 Copy Logs"
                            Command="{Binding CopyLogsCommand}"
                            IsEnabled="{Binding CopyButtonEnabled}"
                            Style="{StaticResource SuccessButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Connection Information Panel -->
        <Border Grid.Row="1"
                Style="{StaticResource ModernCardStyle}"
                Margin="24,16,24,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- IP Address -->
                <Border Grid.Column="0"
                        Style="{StaticResource StatsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="IP ADDRESS"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                        <TextBlock Text="{Binding IpAddress}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   FontFamily="Consolas"
                                   Foreground="{StaticResource SEC_DarkGrey}" />
                        <TextBlock Text="Target device"
                                   FontSize="12"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                    </StackPanel>
                </Border>

                <!-- Port -->
                <Border Grid.Column="1"
                        Style="{StaticResource StatsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="PORT"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                        <TextBlock Text="{Binding Port}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   FontFamily="Consolas"
                                   Foreground="#2563eb" />
                        <TextBlock Text="Connection port"
                                   FontSize="12"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                    </StackPanel>
                </Border>

                <!-- Live Status -->
                <Border Grid.Column="2"
                        Style="{StaticResource StatsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="STATUS"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                        <TextBlock Text="{Binding LiveStatus}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#16a34a" />
                        <TextBlock Text="Current state"
                                   FontSize="12"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                    </StackPanel>
                </Border>

                <!-- Log Count -->
                <Border Grid.Column="3"
                        Style="{StaticResource StatsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="LOG ENTRIES"
                                   FontSize="11"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                        <TextBlock Text="{Binding SummaryLogCount}"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#7c3aed" />
                        <TextBlock Text="Total recorded"
                                   FontSize="12"
                                   Foreground="{StaticResource SEC_MidGrey}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2"
              Margin="24,8,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Protocol Status -->
            <Border Grid.Column="0"
                    Style="{StaticResource ModernCardStyle}"
                    Margin="0">
                <StackPanel>
                    <TextBlock Text="Protocol Status"
                               Style="{StaticResource SectionTitleStyle}"
                               Margin="0,0,0,16" />

                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  MaxHeight="400">
                        <ItemsControl ItemsSource="{Binding ProtocolStatusCards}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ProtocolStatusCardStyle}"
                                            BorderBrush="{Binding BorderColor}"
                                            Margin="0,0,0,8">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal"
                                                        Margin="0,0,0,4">
                                                <Ellipse Width="12"
                                                         Height="12"
                                                         Fill="{Binding StatusColor}"
                                                         Margin="0,0,8,0"
                                                         VerticalAlignment="Center" />
                                                <TextBlock Text="{Binding ProtocolName}"
                                                           Foreground="{StaticResource SEC_DarkGrey}"
                                                           FontWeight="Bold"
                                                           FontSize="14"
                                                           VerticalAlignment="Center" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding StatusText}"
                                                       Foreground="{StaticResource SEC_MidGrey}"
                                                       FontSize="11"
                                                       FontStyle="Italic"
                                                       Margin="20,0,0,0" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Connection Summary -->
                    <Border Style="{StaticResource ConnectionInfoStyle}"
                            Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="Connection Summary"
                                       Style="{StaticResource SectionHeaderStyle}"
                                       Margin="0,0,0,12" />

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Grid.Column="0"
                                           Text="Protocol:"
                                           Style="{StaticResource LabelTextBlockStyle}" />
                                <TextBlock Grid.Row="0"
                                           Grid.Column="1"
                                           Text="{Binding SummaryProtocol}"
                                           Style="{StaticResource ValueTextBlockStyle}" />

                                <TextBlock Grid.Row="1"
                                           Grid.Column="0"
                                           Text="Username:"
                                           Style="{StaticResource LabelTextBlockStyle}" />
                                <TextBlock Grid.Row="1"
                                           Grid.Column="1"
                                           Text="{Binding SummaryUsername}"
                                           Style="{StaticResource ValueTextBlockStyle}" />

                                <TextBlock Grid.Row="2"
                                           Grid.Column="0"
                                           Text="Entries:"
                                           Style="{StaticResource LabelTextBlockStyle}" />
                                <TextBlock Grid.Row="2"
                                           Grid.Column="1"
                                           Text="{Binding SummaryLogCount}"
                                           Style="{StaticResource ValueTextBlockStyle}" />

                                <TextBlock Grid.Row="3"
                                           Grid.Column="0"
                                           Text="Updated:"
                                           Style="{StaticResource LabelTextBlockStyle}" />
                                <TextBlock Grid.Row="3"
                                           Grid.Column="1"
                                           Text="{Binding SummaryLastUpdated}"
                                           Style="{StaticResource ValueTextBlockStyle}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Right Panel - Activity Log -->
            <Border Grid.Column="2"
                    Style="{StaticResource ModernCardStyle}"
                    Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Activity Log Header -->
                    <Grid Grid.Row="0"
                          Margin="0,0,0,16">
                        <TextBlock Text="Live Activity Monitor"
                                   Style="{StaticResource SectionTitleStyle}"
                                   Margin="0" />
                    </Grid>

                    <!-- Activity Log Content -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Protocol Groups -->
                            <ItemsControl ItemsSource="{Binding ProtocolGroups}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SEC_White}"
                                                BorderBrush="#e5e7eb"
                                                BorderThickness="1"
                                                CornerRadius="6"
                                                Margin="0,0,0,16"
                                                Padding="16">
                                            <StackPanel>
                                                <!-- Protocol Header -->
                                                <Grid Margin="0,0,0,12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0"
                                                                Orientation="Horizontal">
                                                        <TextBlock Text="🔗"
                                                                   FontSize="16"
                                                                   Margin="0,0,8,0"
                                                                   VerticalAlignment="Center" />
                                                        <TextBlock Text="{Binding ProtocolName}"
                                                                   Style="{StaticResource ProtocolHeaderStyle}"
                                                                   FontSize="16"
                                                                   Margin="0" />
                                                    </StackPanel>

                                                    <Border Grid.Column="2"
                                                            Background="#dbeafe"
                                                            Padding="6,2"
                                                            CornerRadius="3">
                                                        <TextBlock Text="{Binding LogEntries.Count, StringFormat={}{0} entries}"
                                                                   FontSize="11"
                                                                   Foreground="#1e40af"
                                                                   FontWeight="Bold" />
                                                    </Border>
                                                </Grid>

                                                <!-- Log Entries -->
                                                <ItemsControl ItemsSource="{Binding LogEntries}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Style="{StaticResource LogEntryStyle}">
                                                                <StackPanel>
                                                                    <!-- Main log line -->
                                                                    <DockPanel>
                                                                        <TextBlock Text="{Binding FormattedTimestamp, StringFormat='[{0}]'}"
                                                                                   Style="{StaticResource ModernTextBlockStyle}"
                                                                                   FontFamily="Consolas"
                                                                                   FontSize="10"
                                                                                   Foreground="{StaticResource SEC_MidGrey}"
                                                                                   Margin="0,0,8,0"
                                                                                   DockPanel.Dock="Left" />

                                                                        <TextBlock Style="{StaticResource LogIconStyle}"
                                                                                   DockPanel.Dock="Left" />

                                                                        <TextBlock Text="{Binding Step}"
                                                                                   Style="{StaticResource ModernTextBlockStyle}"
                                                                                   FontFamily="Consolas"
                                                                                   FontSize="11"
                                                                                   TextWrapping="Wrap" />
                                                                    </DockPanel>

                                                                    <!-- Details (if present) -->
                                                                    <TextBlock Text="{Binding Details, StringFormat='    └─ {0}'}"
                                                                               Style="{StaticResource ModernTextBlockStyle}"
                                                                               FontFamily="Consolas"
                                                                               FontSize="10"
                                                                               Foreground="{StaticResource SEC_MidGrey}"
                                                                               Margin="20,2,0,0"
                                                                               TextWrapping="Wrap"
                                                                               Visibility="{Binding Details, Converter={StaticResource StringToVisibilityConverter}}" />
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Empty State -->
                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="0,50">
                                <TextBlock Text="📊"
                                           FontSize="48"
                                           HorizontalAlignment="Center"
                                           Opacity="0.3" />
                                <TextBlock Text="No activity logs available"
                                           FontSize="18"
                                           FontWeight="Medium"
                                           Foreground="{StaticResource SEC_MidGrey}"
                                           HorizontalAlignment="Center"
                                           Margin="0,8,0,4" />
                                <TextBlock Text="Activity will appear here as protocols are tested"
                                           FontSize="14"
                                           Foreground="{StaticResource SEC_MidGrey}"
                                           HorizontalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Action Bar -->
        <Border Grid.Row="3"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,1,0,0">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Ellipse Width="12"
                             Height="12"
                             Fill="#16a34a"
                             Margin="0,0,8,0" />
                    <TextBlock Text="Real-time monitoring active"
                               Foreground="{StaticResource SEC_DarkGrey}"
                               VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="🔄 Refresh"
                            Style="{StaticResource SecondaryButtonStyle}" />
                    <Button Content="❌ Close"
                            Command="{Binding CloseCommand}"
                            Style="{StaticResource PrimaryButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>