<Window x:Class="wpfhikip.Views.Dialogs.CameraInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:wpfhikip.Converters"
        mc:Ignorable="d"
        Title="Camera Information"
        Height="900"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600"
        MinWidth="800">

    <Window.Resources>
        <!-- Add the converters -->
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid Background="#f8fafc">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,0,0,1">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding IpAddress, StringFormat='Camera Information - {0}'}"
                               Style="{StaticResource HeaderTextBlockStyle}" />
                    <TextBlock Text="Real-time device monitoring and configuration details"
                               Style="{StaticResource SubHeaderTextBlockStyle}" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="🔄 Refresh"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource SuccessButtonStyle}"
                            Margin="0,0,8,0"
                            Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

                    <Button Content="⏹️ Cancel"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource DangerButtonStyle}"
                            Margin="0,0,8,0"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <Button Content="📋 Copy All"
                            Command="{Binding CopyAllCommand}"
                            Style="{StaticResource SuccessButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Status -->
        <Border Grid.Row="1"
                Style="{StaticResource ModernCardStyle}"
                Margin="24,16,24,8"
                Background="#dbeafe"
                BorderBrush="#3b82f6"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <TextBlock Text="🔄"
                           FontSize="16"
                           Margin="0,0,10,0"
                           Foreground="#1e40af">
                    <TextBlock.RenderTransform>
                        <RotateTransform x:Name="LoadingRotation" />
                    </TextBlock.RenderTransform>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                     Storyboard.TargetProperty="Angle"
                                                     From="0"
                                                     To="360"
                                                     Duration="0:0:1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
                <TextBlock Text="{Binding LoadingStatus}"
                           Style="{StaticResource LoadingStyle}"
                           Foreground="#1e40af" />
            </StackPanel>
        </Border>

        <!-- Error Message -->
        <Border Grid.Row="1"
                Style="{StaticResource ModernCardStyle}"
                Margin="24,16,24,8"
                Background="#fee2e2"
                BorderBrush="#ef4444"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <TextBlock Text="⚠️"
                           FontSize="16"
                           Margin="0,0,10,0" />
                <TextBlock Text="{Binding ErrorMessage}"
                           Style="{StaticResource ErrorStyle}" />
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2"
              Margin="24,8,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel -->
            <StackPanel Grid.Column="0">
                <!-- Device Information -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="📷 Device Information"
                                   Style="{StaticResource SectionTitleStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="Manufacturer:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="0"
                                     Grid.Column="1"
                                     Text="{Binding Manufacturer, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Model:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="1"
                                     Grid.Column="1"
                                     Text="{Binding Model, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="Firmware:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="2"
                                     Grid.Column="1"
                                     Text="{Binding Firmware, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="3"
                                       Grid.Column="0"
                                       Text="Serial Number:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="3"
                                     Grid.Column="1"
                                     Text="{Binding SerialNumber, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="4"
                                       Grid.Column="0"
                                       Text="MAC Address:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="4"
                                     Grid.Column="1"
                                     Text="{Binding MacAddress, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Network Settings -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Margin="0">
                    <StackPanel>
                        <TextBlock Text="🌐 Network Settings"
                                   Style="{StaticResource SectionTitleStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="Current IP:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="0"
                                     Grid.Column="1"
                                     Text="{Binding CurrentIp, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Subnet Mask:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="1"
                                     Grid.Column="1"
                                     Text="{Binding SubnetMask, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="Gateway:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="2"
                                     Grid.Column="1"
                                     Text="{Binding Gateway, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="3"
                                       Grid.Column="0"
                                       Text="DNS1:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="3"
                                     Grid.Column="1"
                                     Text="{Binding Dns1, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="4"
                                       Grid.Column="0"
                                       Text="DNS2:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="4"
                                     Grid.Column="1"
                                     Text="{Binding Dns2, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Panel -->
            <StackPanel Grid.Column="2">
                <!-- Connection Information -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="🔗 Connection Information"
                                   Style="{StaticResource SectionTitleStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="IP Address:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="0"
                                     Grid.Column="1"
                                     Text="{Binding IpAddress, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Port:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="1"
                                     Grid.Column="1"
                                     Text="{Binding Port, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="Protocol:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="2"
                                     Grid.Column="1"
                                     Text="{Binding Protocol, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="3"
                                       Grid.Column="0"
                                       Text="Username:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="3"
                                     Grid.Column="1"
                                     Text="{Binding Username, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="4"
                                       Grid.Column="0"
                                       Text="Status:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="4"
                                     Grid.Column="1"
                                     Text="{Binding Status, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Video Stream Information -->
                <Border Style="{StaticResource ModernCardStyle}"
                        Margin="0">
                    <StackPanel>
                        <TextBlock Text="🎥 Video Stream Information"
                                   Style="{StaticResource SectionTitleStyle}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="Main Stream:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="0"
                                     Grid.Column="1"
                                     Text="{Binding MainStream, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Sub Stream:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="1"
                                     Grid.Column="1"
                                     Text="{Binding SubStream, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="Codec Type:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="2"
                                     Grid.Column="1"
                                     Text="{Binding CodecType, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="3"
                                       Grid.Column="0"
                                       Text="Resolution:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="3"
                                     Grid.Column="1"
                                     Text="{Binding Resolution, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="4"
                                       Grid.Column="0"
                                       Text="Quality Control:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="4"
                                     Grid.Column="1"
                                     Text="{Binding QualityControlType, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="5"
                                       Grid.Column="0"
                                       Text="Frame Rate:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="5"
                                     Grid.Column="1"
                                     Text="{Binding FrameRate, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />

                            <TextBlock Grid.Row="6"
                                       Grid.Column="0"
                                       Text="Bit Rate:"
                                       Style="{StaticResource LabelTextBlockStyle}" />
                            <TextBox Grid.Row="6"
                                     Grid.Column="1"
                                     Text="{Binding BitRate, Mode=OneWay}"
                                     Style="{StaticResource SelectableValueTextStyle}" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Action Bar -->
        <Border Grid.Row="3"
                Background="{StaticResource SEC_White}"
                BorderBrush="{StaticResource SEC_LightGrey}"
                BorderThickness="0,1,0,0">
            <Grid Margin="24,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Ellipse Width="12"
                             Height="12"
                             Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill"
                                        Value="#6b7280" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}"
                                                 Value="True">
                                        <Setter Property="Fill"
                                                Value="#3b82f6" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HasError}"
                                                 Value="True">
                                        <Setter Property="Fill"
                                                Value="#ef4444" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HasError}"
                                                 Value="False">
                                        <Setter Property="Fill"
                                                Value="#16a34a" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock VerticalAlignment="Center"
                               Foreground="{StaticResource SEC_DarkGrey}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text"
                                        Value="Ready - Use refresh button to update information" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}"
                                                 Value="True">
                                        <Setter Property="Text"
                                                Value="Loading device information..." />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HasError}"
                                                 Value="True">
                                        <Setter Property="Text"
                                                Value="Error occurred while loading" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="❌ Close"
                            Click="CloseButton_Click"
                            Style="{StaticResource PrimaryButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>